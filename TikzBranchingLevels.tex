\begin{figure}[h!]
\centering
\resizebox{0.92\columnwidth}{!}{%
\begin{tikzpicture}[>=Latex, thick, node distance=6mm,
  lvl/.style={rectangle, rounded corners, draw=black!60, fill=black!3,
              text width=46mm, align=left, inner sep=4pt, font=\footnotesize},
  node1/.style={circle, draw, minimum size=5mm, inner sep=0pt, font=\tiny},
  lab/.style={font=\tiny, align=left}]

  % --- the hierarchy, shallowest fractional level first ---
  \node[lvl] (l1) {\textbf{1. Dispatch total}\\[1pt]
     $\sum_{p}\lambda^{g}_{pt}\notin\mathbb{Z}$\\[1pt]
     integer in every plan; every column of the period is on one side
     of the row, so the split partitions \emph{plans}};
  \node[lvl, below=of l1] (l2) {\textbf{2. Delivery totals}\\[1pt]
     $Q_{nt}$, $Q^{r}_{rt}\notin\mathbb{Z}$\\[1pt]
     original variables counting physical units; bounding one tightens
     a row that already carries a dual};
  \node[lvl, below=of l2] (l3) {\textbf{3. Column variable}\\[1pt]
     $\lambda^{g}_{pt}\notin\mathbb{Z}$ \quad\emph{(last resort)}\\[1pt]
     has a near-identical twin, so the LP answers by moving the
     fractionality rather than losing it};
  \draw[->] (l1) -- node[right, lab] {all integral} (l2);
  \draw[->] (l2) -- node[right, lab] {all integral} (l3);

  % --- the tree itself ---
  \begin{scope}[xshift=62mm, yshift=-6mm]
    \node[node1] (r) at (0,0) {};
    \node[node1] (a) at (-1.6,-1.2) {}; \node[node1] (b) at (1.6,-1.2) {};
    \node[node1] (c) at (-2.6,-2.4) {}; \node[node1] (d) at (-0.6,-2.4) {};
    \node[node1, fill=black!12] (e) at (0.9,-2.4) {}; \node[node1] (f) at (2.5,-2.4) {};
    \draw[->] (r) -- node[above left, lab] {$\le\lfloor v\rfloor$} (a);
    \draw[->] (r) -- node[above right, lab] {$\ge\lceil v\rceil$} (b);
    \draw[->] (a) -- (c); \draw[->] (a) -- (d);
    \draw[->] (b) -- (e); \draw[->] (b) -- (f);
    \node[lab, anchor=west] at (3.0,-1.2) {both children kept};
    \node[lab, anchor=west] at (3.0,-2.4) {cheapest relaxation\\expanded next};
    \node[lab, anchor=north, text width=52mm, align=center] at (0,-3.2)
      {every node re-prices, so columns are created \emph{after} the
       branching decisions; every $20$ nodes the integer master is
       solved over the pool to supply an incumbent, without which
       nothing can be pruned};
  \end{scope}
\end{tikzpicture}}
\caption{Branching in MATH-3D-SCPTOP. The shallowest fractional level
is branched on, so the search works on aggregates that are integral in
any feasible plan before it touches the master's own variables. Under
the column-variable rule alone, one instance was branched $3000$ times
to depth $1971$ without ever reaching an integral relaxation; with the
hierarchy, $90$ branchings on a representative run split $61$ on
delivery totals, $29$ on dispatch totals and none on a column
variable.}
\label{fig:branching}
\end{figure}
